@model SistemaPerfiladoCriminal.Models.Autor

@{
    ViewBag.Title = "Detalles";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.LstrNombre</h2>

<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">Detalle</th>
            <th scope="col">Ver</th>
            <th scope="col">Editar</th>
            <th scope="col">Eliminar</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var detalle in Model.LcolDetalles.ToList())
        {
        <tr>
            <td>
                @detalle.LstrDetalle
            </td>
            <td>
                @Html.ActionLink("Detalles", "../Detalles/Details", new { id = detalle.LintId })
            </td>
            <td>
                @Html.ActionLink("Editar", "../Detalles/Edit", new { id = detalle.LintId })
            </td>
            <td>
                @Html.ActionLink("Eliminar", "../Detalles/Delete", new { id = detalle.LintId })
            </td>
        </tr>
        }
    </tbody>
</table>
