@model SistemaPerfiladoCriminal.Models.DetallesAutor

@{
    ViewBag.Title = "Crear";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!--
@{
    var autores = (List<SelectListItem>)ViewData["autores"];
}
-->

<h2>Crear</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">
    <h4>Detalles de autor</h4>
    <hr />

    <!--
    <div class="form-group">
        <label>Seleccione el autor</label>
        @Html.DropDownListFor(model => model.LAutor.LintId,
        autores,
        "Seleccion el autor",
        new { @class = "form-control" })
    </div>
        -->

    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="form-group">
        <label class="control-label col-md-2">Detalle</label>
        <div class="col-md-10">
            @Html.EditorFor(model => model.LstrDetalle, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.LstrDetalle, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Crear" class="btn btn-default" />
        </div>
    </div>
</div>
}