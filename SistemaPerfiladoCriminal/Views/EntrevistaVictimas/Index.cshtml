@model IEnumerable<SistemaPerfiladoCriminal.Models.EntrevistaVictima>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LstrNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LstrDescripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LstrNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LstrDescripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LintId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LintId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LintId })
        </td>
    </tr>
}

</table>
